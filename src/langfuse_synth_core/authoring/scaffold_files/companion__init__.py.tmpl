"""Companion-app stub (emitted by `synth-authoring new --companion`).

Full companion authoring is Spec G — this is only a placeholder seam so a kit that will
grow a live companion surface has somewhere to start.
"""
