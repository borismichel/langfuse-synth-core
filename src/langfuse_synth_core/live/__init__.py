"""Scenario-agnostic UI primitives for a kit's live playground.

The shared web chrome — the Langfuse design tokens + page shell (``theme``) and the
prefix-aware internal paths driven by ``LIVE_BASE_PATH`` (``paths``). The scenario's
actual pages, forms, and routes live in each kit's ``live`` package.
"""
