"""Seed-time synthesis primitives shared by every kit.

Currently the batch-ingestion event-envelope builders (``events``): the machine that
speaks the Langfuse ingestion data model. The scenario-shaped seed subsystems
(traces, scores, generator, run orchestration) live in each kit.
"""
